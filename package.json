{
  "scripts": {
    "build": "tailwindcss build src/assets/style.css -o dist/assets/style.css",
    "watch": "tailwindcss build src/assets/style.css -o dist/assets/style.css --watch",
    "prepare": "npm-run-all build --silent"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.2.7"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.3.0"
  }
}
